SELECT
    be.id            AS warning_id,
    be.bill_record_id,
    be.message       AS bill_warning,
    be.severity,
    be.resolved,
    be.validation_check_id
FROM bill_management_v2.bill_errors be
LIMIT 100