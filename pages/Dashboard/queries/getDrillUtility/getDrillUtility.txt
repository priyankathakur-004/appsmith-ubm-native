-- Utility-type options for the drill filter (current customer).
SELECT utility_type AS value, utility_type AS label
FROM bill_management_v2.analytics_monthly_feed
WHERE customer_id = {{ CustomerSelect.selectedOptionValue || 76013 }}
GROUP BY utility_type
ORDER BY utility_type;
