{
  "gitSyncId": "6a42383420f165a7650f6c99_err00009-0000-4000-8000-000000000009",
  "id": "Errors_fetch_comm_detail",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n    eds.id                                          AS id,\n    eds.email_address                               AS email_address,\n    eds.email_type                                  AS module,\n    eds.email_subject                               AS email_subject,\n    eds.sendgrid_tracking_id                        AS tracking_id,\n    cs.name                                         AS customer,\n    TO_CHAR(eds.created_at, 'MM/DD/YYYY HH24:MI')    AS created_at,\n    TO_CHAR(eds.updated_at, 'MM/DD/YYYY HH24:MI')    AS updated_at,\n    TO_CHAR(eds.processed,  'MM/DD/YYYY HH24:MI')    AS processed,\n    TO_CHAR(eds.delivered,  'MM/DD/YYYY HH24:MI')    AS delivered,\n    TO_CHAR(eds.open,       'MM/DD/YYYY HH24:MI')    AS opened,\n    TO_CHAR(eds.bounce,     'MM/DD/YYYY HH24:MI')    AS bounce,\n    TO_CHAR(eds.dropped,    'MM/DD/YYYY HH24:MI')    AS dropped,\n    TO_CHAR(eds.deferred,   'MM/DD/YYYY HH24:MI')    AS deferred,\n    TO_CHAR(eds.blocked,    'MM/DD/YYYY HH24:MI')    AS blocked,\n    TO_CHAR(COALESCE(eds.bounce, eds.dropped, eds.blocked, eds.deferred, eds.updated_at, eds.created_at), 'MM/DD/YYYY HH24:MI') AS status_change_date,\n    CASE\n      WHEN eds.bounce   IS NOT NULL THEN 'Bounce'\n      WHEN eds.dropped  IS NOT NULL THEN 'Dropped'\n      WHEN eds.blocked  IS NOT NULL THEN 'Blocked'\n      WHEN eds.deferred IS NOT NULL THEN 'Deferred'\n      ELSE 'None'\n    END                                             AS status,\n    CASE\n      WHEN eds.bounce IS NOT NULL OR eds.dropped IS NOT NULL OR eds.blocked IS NOT NULL THEN 'Error'\n      WHEN eds.deferred IS NOT NULL THEN 'Warning'\n      ELSE 'Information'\n    END                                             AS type\nFROM bill_management_v2.emails_deliveries_status eds\nLEFT JOIN bill_management_v2.customers_search cs ON cs.id = eds.customer_id\nWHERE eds.id = {{ CommTable.model.selectedRow ? CommTable.model.selectedRow.id : 0 }}\nLIMIT 1;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": false
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "UBM_Database",
      "isAutoGenerated": false,
      "name": "UBM_Database",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "fetch_comm_detail",
    "pageId": "Errors",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}
