{
  "gitSyncId": "6a42383420f165a7650f6c99_err00007-0000-4000-8000-000000000007",
  "id": "Errors_fetch_comm_statuses",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT DISTINCT status FROM (\n  SELECT CASE\n    WHEN bounce   IS NOT NULL THEN 'Bounce'\n    WHEN dropped  IS NOT NULL THEN 'Dropped'\n    WHEN blocked  IS NOT NULL THEN 'Blocked'\n    WHEN deferred IS NOT NULL THEN 'Deferred'\n  END AS status\n  FROM bill_management_v2.emails_deliveries_status\n  WHERE deleted_at IS NULL\n    AND (bounce IS NOT NULL OR dropped IS NOT NULL OR blocked IS NOT NULL OR deferred IS NOT NULL)\n) s\nWHERE status IS NOT NULL\nORDER BY status;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": false
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "UBM_Database",
      "isAutoGenerated": false,
      "name": "UBM_Database",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [],
    "name": "fetch_comm_statuses",
    "pageId": "Errors",
    "runBehaviour": "AUTOMATIC",
    "userSetOnLoad": false
  }
}
