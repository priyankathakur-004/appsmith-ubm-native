{
  "gitSyncId": "loc_fetch_locattr_0001",
  "id": "Locations_fetch_location_attributes",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n    ROW_NUMBER() OVER (ORDER BY attribute_name) AS \"Sort Order\",\n    attribute_name   AS \"Name\",\n    attribute_format AS \"Type\",\n    attribute_uom    AS \"UoM\"\nFROM (\n    SELECT DISTINCT attribute_name, attribute_format, attribute_uom\n    FROM bill_management_v2.custom_location_attributes\n    WHERE customer_id = {{ CustomerSelect.selectedOptionValue || 0 }}\n) t\nORDER BY attribute_name;\n",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": false
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "UBM_Database",
      "isAutoGenerated": false,
      "name": "UBM_Database",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "fetch_location_attributes",
    "pageId": "Locations",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": true
  }
}
