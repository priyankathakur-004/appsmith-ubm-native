SELECT {{ ReportSpecs.selectedSpec().select }}
FROM {{ ReportSpecs.selectedSpec().from }}
{{ ReportSpecs.clauses() }}
{{ ReportSpecs.selectedSpec().orderBy ? 'ORDER BY ' + ReportSpecs.selectedSpec().orderBy : '' }}
LIMIT {{ Math.max(1, (appsmith.store.reportsPageEnd || 100) - (appsmith.store.reportsPageStart || 0)) }}
OFFSET {{ Math.max(0, appsmith.store.reportsPageStart || 0) }};
