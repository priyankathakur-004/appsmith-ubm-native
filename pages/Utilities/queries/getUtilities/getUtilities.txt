SELECT DISTINCT commodity AS utility
FROM bill_management_v2."bill_items"
{{ CustomerSelect.selectedOptionValue
    ? "WHERE customer_id = " + parseInt(CustomerSelect.selectedOptionValue, 10)
    : ""
}}
ORDER BY utility;
